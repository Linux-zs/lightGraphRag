# KG 抽取性能验证

日期：2026-09-06。本机已配置接口，合成文本，LightRAG JSON 抽取提示词，1536 输出 token 上限，关闭思考，gleaning=0，不启用应用结果缓存。所有结果仅代表本次样本和服务负载，不是供应商 SLA。

## 同一中文样本对照

| 模型 | 完整请求及解析耗时 | 预期核心关系命中 |
| --- | --- | --- |
| SiliconFlow Qwen/Qwen2.5-7B-Instruct（原绑定） | 60 秒超时 | 无结果 |
| DeepSeek 官方 deepseek-v4-flash | 3.142 秒 | 5/5 |
| SiliconFlow Qwen/Qwen3-30B-A3B-Instruct-2507 | 32.452 秒 | 5/5 |
| SiliconFlow Pro/Qwen/Qwen2.5-7B-Instruct | 39.225 秒 | 0/5，SDK 无有效实体关系 |

另一次 DeepSeek Flash 三类样本验证：部署依赖 2.906 秒、供应链历史事实 1.765 秒、混合配置/日志文本 3.133 秒，核心关系分别命中 5/5、2/2、4/4。八路并发真实调用经应用逐块调度处理，8/8 块完成，无跳过，总耗时 4.224 秒；单次请求 1.633–3.848 秒。

核心关系命中是已知实体对匹配，不代表所有描述、时间条件和额外实体都正确。混合配置样本仍出现日志编号实体等噪声。性能和降噪质量必须分别评价，不以清空或少抽图谱换取速度。

## 本轮处理

- 保留逐块失败隔离，正常块不因其他块失败而取消重跑。
- KG 仅保留应用层重试预算：默认最多两次，空 JSON 的兼容降级也占用一次。真实 SDK 配合模拟 HTTP 验证，持续 500 从原来的 9 次请求降至 2 次，空内容从 6 次降至 2 次。
- 每次请求记录独立调用编号、尝试、状态、耗时；本地队列与块处理耗时分别保留。不开启正文或凭据日志。
- 本机 KG 绑定切换至已配置的 DeepSeek 官方 Flash；问答、嵌入和 Rerank 不变，并发保留 8。模型绑定位于本机数据配置，不会随 Git 自动同步。

## 复测

运行 `uv run python scripts/benchmark_kg.py --live --candidate "<profile_id>=<model_id>" --samples 3`。可以重复传入 `--candidate` 顺序比较模型；此命令会产生 API 费用，但不读取业务文档或写入知识库。更换机器或供应商后应重新验证，不能将本次几十倍差异直接外推到所有真实文档。
